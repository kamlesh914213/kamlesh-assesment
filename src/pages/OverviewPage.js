import React from "react";
import DataText from "../components/dataText"; // Corrected path

const OverviewPage = () => {
  return (
    <div className="container">
      <DataText />
    </div>
  );
};

export default OverviewPage;
